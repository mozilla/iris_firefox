

[pytest]
log_cli=false
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
addopts = -p no:warnings --no-print-logs
markers =
    details:

#test discovering
testpaths=tests
python_files = *.py
python_classes = Test
python_functions = *run


